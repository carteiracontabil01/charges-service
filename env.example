# Configuração da Aplicação
PORT=8083

# Supabase
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_KEY=sua_service_role_key_aqui

# Segmented schemas:
# - domain tables are under schema `iam`.
SUPABASE_SCHEMA=iam

# Asaas
# Ex:
# - HML: https://api-sandbox.asaas.com
# - PRD: https://api.asaas.com
# (o charges-service lê base_api/token da tabela iam.billing_integrations)

# Asaas Webhook Secret
# Token de acesso para validar webhooks do Asaas (configure o mesmo valor no painel do Asaas)
# Gere um UUID v4 forte: https://www.uuidgenerator.net/version4
# Configure este token no header "asaas-access-token" ao criar o webhook no Asaas
ASAAS_WEBHOOK_SECRET=seu_token_secreto_aqui_uuid_v4

# Webhooks - NFSe Municipal
# URL que a Focus vai chamar quando uma NFSe for processada
WEBHOOK_URL=https://seu-dominio.com/focus/nfse

# Ambiente
ENVIRONMENT=development

# Swagger / OpenAPI (em PRD, configure para não apontar para localhost)
# Ex:
# SWAGGER_HOST=api.minhaempresa.com.br
# SWAGGER_SCHEMES=https
# IMPORTANTE:
# - SWAGGER_HOST deve ser apenas host[:porta] (sem http/https e sem barra final)
# - SWAGGER_SCHEMES define o(s) esquema(s) usados pelo Swagger UI
#
# PRD (ATIVO) - ajuste para o domínio do charges-service quando publicar
SWAGGER_HOST=api-charges.carteiracontabil.com
SWAGGER_SCHEMES=https
#
# Local (descomente para rodar local)
# SWAGGER_HOST=localhost:8083
# SWAGGER_SCHEMES=http

# CORS
# Ex: CORS_ALLOWED_ORIGINS=https://app.minhaempresa.com.br,https://admin.minhaempresa.com.br
CORS_ALLOWED_ORIGINS=