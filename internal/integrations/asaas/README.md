# Asaas integration

This folder will host the Asaas integration (API client, models, and webhook handlers).

Planned:
- `client.go` (HTTP client, auth header, retry)
- `charges.go` (create/update/cancel)
- `webhooks.go` (signature validation, event parsing)

